## Personal Local Computer Backup Scripts

Includes:
 * `backup.sh` for main linux computer.
 * `macbackup.sh` for hackintosh.
 * `mediaBackup.sh` for media server.

Also includes `btrfsMaintenance.sh` for btrfs scrub/balance maintenance on main linux computer.
